# CivitAI API Configuration
# Required: Get your API token from https://civitai.com/user/account
CIVITAI_API_TOKEN=your_civitai_api_token_here

# Optional: Default model to use for image generation
# You can find model URNs on CivitAI model pages
# official SDXL
DEFAULT_MODEL="urn:air:sdxl:checkpoint:civitai:101055@128078"
DEFAULT_NEGATIVE_PROMPT="easynegative, badhandv4, (worst quality, low quality, normal quality), bad-artist, blurry, ugly, ((bad anatomy)),((bad hands)),((bad proportions)),((duplicate limbs)),((fused limbs)),((interlocking fingers)),((poorly drawn face)), signature, watermark, artist logo, patreon logo"

# INPUT_TEMPLATE="score_9,score_8_up,score_7_up, masterpiece, best quality, very aesthetic, {prompt}"
INPUT_TEMPLATE="{prompt}"

# Server Configuration
# Host to bind to (0.0.0.0 for all interfaces)
HOST=0.0.0.0

# Port to run the server on
PORT=8000

# Job Polling Configuration
# Maximum number of polling attempts (each attempt is POLL_INTERVAL seconds)
MAX_POLL_ATTEMPTS=150

# Seconds to wait between polling attempts
POLL_INTERVAL=2
